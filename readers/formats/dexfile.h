#ifndef DEXFILE_H
#define DEXFILE_H

#include "dex.h"

class DexFile
{
public:

private:
    // TODO: make the reader a friend class, and inject dependencies to here e.g. reader and cache/load as UI requests
    DexFile();
};

#endif // DEXFILE_H
